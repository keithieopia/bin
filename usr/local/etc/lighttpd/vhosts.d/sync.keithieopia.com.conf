$HTTP["host"] == "sync.keithieopia.com" {
  var.server_name = "sync.keithieopia.com"
  server.name = server_name

  accesslog.filename   = log_root + "/access-" + server_name + ".log"

  proxy.server = ( "" =>
	(( "host" => "0.0.0.0", "port" => "8384" ))
  )
}
